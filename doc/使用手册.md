# 定制码匠（CraftCoder）使用手册



---

## 目录

1. [CLI 快速开始](#1-cli-快速开始)
2. [TUI 界面操作指南](#2-tui-界面操作指南)
3. [代码编辑工作流](#3-代码编辑工作流)
4. [VS Code 扩展使用](#4-vs-code-扩展使用)
5. [Web API 使用](#5-web-api-使用)
6. [故障排除](#6-故障排除)

---

## 1. CLI 快速开始

### 1.1 基础命令

定制码匠提供三个主要交互模式：

```bash
# 交互式 TUI 模式（推荐日常使用）
code-agent tui

# 带初始提示的 TUI 模式（启动后自动发送提示）
code-agent tui "修复 src/auth.rs 中的空指针异常"

# 非交互式执行模式（适合 CI/CD 和脚本）
code-agent exec "修复 src/auth.rs 中的空指针异常"

# App Server 模式（为 IDE 插件提供后端服务）
code-agent app-server
```

### 1.2 三条命令一分钟上手

```bash
# 1. 检查环境
code-agent doctor
# 输出: ✅ All checks passed.

# 2. 进入项目目录并启动 TUI
cd my-project
code-agent tui

# 3. 开始对话：直接输入你的编码需求
💬 > 帮我给 User 模型添加 created_at 和 updated_at 字段

# 或者启动时直接指定任务
code-agent tui "帮我给 User 模型添加 created_at 和 updated_at 字段"
```

### 1.3 Exec 模式（非交互式）

适合 CI/CD 流水线、Shell 脚本集成、批量处理：

```bash
# 基础用法
code-agent exec "任务描述"

# 指定输出格式
code-agent exec -o json "分析当前项目的依赖关系"

# 使用 Plan 引擎（自动分解任务并并行执行）
code-agent exec --plan "重构 UserService 的缓存逻辑"

# 从 stdin 管道读取任务
echo "检查代码中的 SQL 注入漏洞" | code-agent exec

# 管道输出到文件
code-agent exec "生成项目架构文档" > architecture-report.md
```

**退出码**：

| 退出码 | 含义 |
|--------|------|
| 0 | 任务成功完成 |
| 1 | Agent 执行出错 |
| 2 | 工具调用失败 |
| 3 | 超时 |

### 1.4 配置管理

```bash
# 初始化配置
code-agent config init            # 生成项目级默认配置
code-agent config init --target global  # 生成用户级配置

# 查看配置
code-agent config show            # 显示所有生效配置

# 查看单个配置项
code-agent config get model

# 修改配置
code-agent config set model gpt-4o
code-agent config set timeout_secs 120

# 查看配置路径
code-agent config path
```

### 1.5 环境变量配置

除了配置文件，你也可以通过环境变量快速配置：

```bash
# 必需：API 密钥和模型名称
export LLM_API_KEY="sk-your-api-key"
export LLM_CHAT_MODEL="gpt-4o"

# 可选：API 地址（默认 https://api.openai.com/v1）
export LLM_API_BASE_URL="https://your-custom-api/v1"

# 可选：其他模型
export LLM_EMBEDDING_MODEL="text-embedding-3-small"
export LLM_VL_MODEL="gpt-4o"
```

---

## 2. TUI 界面操作指南

### 2.1 界面布局

启动 `code-agent tui` 后，终端分为三个区域：

```
┌──────────────────────────────────────────────────────┐
│  🧠 定制码匠 · CraftCoder              Session: abc123 │  ← 状态栏
├──────────────────────────────────────────────────────┤
│                                                      │
│  🤖 你好！我是定制码匠，你的 AI 编程助手。              │
│     当前项目：user-service（Java/Spring Boot）          │
│                                                      │
│  💬 > 帮我分析 UserController 的潜在性能问题            │
│                                                      │
│  [Thinking] 正在分析 UserController.java...            │
│  - 扫描到 12 个接口方法                                │
│  - L239: 在循环内执行数据库查询，建议批量查询            │
│                                                      │
├──────────────────────────────────────────────────────┤
│  💬 > _                                          │  ← 输入栏
└──────────────────────────────────────────────────────┘
```

### 2.2 快捷键一览

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| **Enter** | 发送消息 | 当前输入内容作为对话消息发送 |
| **Ctrl+C** | 中断当前操作 | 中断正在执行的 Agent 循环或工具调用 |
| **Ctrl+D** | 退出 TUI | 保存当前会话并退出 |
| **Ctrl+L** | 清屏 | 清空聊天面板显示（不清除历史）|
| **↑ / ↓** | 历史消息 | 浏览之前发送的消息 |
| **Tab** | 命令补全 | 输入 `/` 后触发命令补全 |
| **PageUp / PageDown** | 滚动聊天面板 | 上下滚动查看更多历史 |

### 2.3 斜杠命令（Command Palette）

在输入框中输入 `/` 触发命令面板：

| 命令 | 说明 | 示例 |
|------|------|------|
| `/edit` | 切换为编辑模式，对指定文件进行修改 | `/edit src/auth.rs` |
| `/debug` | 切换为调试模式，分析运行时错误 | `/debug --trace src/service/user.rs` |
| `/review` | 触发代码审查 | `/review --pr 42` |
| `/config` | 修改当前会话配置 | `/config model.temperature 0.6` |
| `/context` | 查看当前上下文使用情况 | `/context` |
| `/clear` | 重置对话上下文 | `/clear` |
| `/save` | 保存当前会话为 markdown | `/save review-notes.md` |
| `/export` | 导出对话历史（JSON 格式） | `/export session-abc123.json` |
| `/help` | 显示帮助信息 | `/help` |

### 2.4 状态栏信息

状态栏从左到右显示：

| 项目 | 说明 |
|------|------|
| 🧠 | Agent 名称和品牌 |
| Session ID | 当前会话标识符（前 8 位）|
| 📊 12345/128000 | 当前使用 Token / 总 Token 预算 |
| 🔄 Turn 3/30 | 当前循环轮次 / 最大轮次 |
| 🛡️ Guard: ON | 安全审查状态（ON/WARN/OFF）|
| 🔧 6 tools | 可用工具数量 |

### 2.5 理解 ReAct 循环（Agent 收到消息后发生了什么？）

当你发送一条消息给 Agent 时，它内部经历了一个叫做 **ReAct 循环**（Reasoning + Acting）的过程。理解这个过程有助于你更好地使用 CraftCoder。

**一次完整的 ReAct 循环包含以下步骤：**

```
你发送消息
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. 思考（Reasoning）                                  │
│    Agent 分析你的问题，决定需要做什么。                  │
│    例如："用户想修复空指针异常，我需要先读取那个文件。"    │
└─────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 2. 行动（Acting）                                     │
│    Agent 调用一个工具来执行操作。                       │
│    例如：read_file("src/auth/login.rs")               │
└─────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 3. 观察（Observing）                                   │
│    Agent 接收工具的执行结果。                           │
│    例如：文件内容被返回给 Agent。                       │
└─────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 4. 判断（Judging）                                     │
│    Agent 判断问题是否已解决：                            │
│    - 如果已解决 → 生成最终回答                          │
│    - 如果未解决 → 回到步骤 1，继续循环                   │
└─────────────────────────────────────────────────────┘
    │
    ▼
你看到最终回答
```

**一个真实的例子：**

```
你: "修复 src/auth/login.rs 中的空指针异常"

Agent 内部循环：
  Turn 1:
    思考 → "我需要先看代码"
    行动 → read_file("src/auth/login.rs")
    观察 → 看到第 42 行 user.getName() 可能为 null
    判断 → "找到问题了，但需要确认调用方"

  Turn 2:
    思考 → "让我看看谁调用了这个方法"
    行动 → grep("user.getName", "src/")
    观察 → 发现 3 处调用都没有做 null 检查
    判断 → "问题已定位，开始修复"

  Turn 3:
    思考 → "添加 null 检查并返回友好错误"
    行动 → edit_file("src/auth/login.rs", ...)
    观察 → 编辑成功
    判断 → "修复完成"

Agent 输出: "已修复第 42 行的空指针异常。添加了 null 检查..."
```

**这对你意味着什么？**

- Agent 可能需要多轮才能完成复杂任务，这是正常的
- 你可以在 TUI 中实时看到 Agent 的思考过程和工具调用
- 如果 Agent 走偏了，你可以随时用 Ctrl+C 中断并纠正它
- 提供清晰的上下文可以减少 Agent 的循环次数

---

## 3. 代码编辑工作流

### 3.1 标准工作流：生成 → 审查 → 接受/拒绝

定制码匠的代码编辑采用三步确认流程，确保开发者始终拥有最终控制权：

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ 1. 生成      │ → │ 2. Diff 预览  │ → │ 3. 确认      │
│ AI 分析需求   │    │ 展示变更内容  │    │ 接受/拒绝/修改 │
│ 生成代码     │    │ 逐文件对比    │    │ 一键回滚     │
└─────────────┘    └─────────────┘    └─────────────┘
```

#### 步骤 1：描述需求

```
💬 > 在 UserService 中添加登录失败次数限制，5 分钟内失败 5 次锁定账户 30 分钟
```

#### 步骤 2：审查 Diff

Agent 生成代码后，展示变更的 Diff 预览：

```
📝 变更预览（2 个文件）:

  src/service/UserService.java  (+35 lines, -2 lines)
  src/config/RedisConfig.java   (+12 lines, new file)

  ─── Diff: src/service/UserService.java ───
  + @Autowired
  + private RedisTemplate<String, Integer> redisTemplate;
  +
  + private static final int MAX_ATTEMPTS = 5;
  + private static final int WINDOW_MINUTES = 5;
  + private static final int LOCK_MINUTES = 30;
  +
  + public LoginResult login(String username, String password) {
  +     String key = "login:attempts:" + username;
  +     Integer attempts = redisTemplate.opsForValue().get(key);
  +     ...
```

#### 步骤 3：确认操作

```
操作选项:
  [A] 接受全部变更  [R] 拒绝全部变更  [F] 逐文件审查  [M] 修改后重试
```

### 3.2 支持的文件操作

Agent 通过以下工具操作文件系统：

| 工具 | 功能 | 权限级别 |
|------|------|:---:|
| `read_file` | 读取文件内容（支持指定行范围） | 🟢 低风险 |
| `write_file` | 创建或覆盖文件 | 🟡 中风险 |
| `edit_file` | 精确编辑（search-replace 模式） | 🟡 中风险 |
| `list_dir` | 列出目录内容 | 🟢 低风险 |
| `grep` | 正则表达式搜索文件内容 | 🟢 低风险 |
| `glob` | 文件名模式匹配搜索 | 🟢 低风险 |

### 3.3 Exec 模式下的代码生成

```bash
# 生成代码并直接写入文件（需确认）
code-agent exec "在 src/utils/ 下创建 DateHelper.java 工具类"

# 仅预览变更，不写入文件（Dry Run）
code-agent exec "重构 UserService" --dry-run

# 使用 Plan 引擎自动分解任务
code-agent exec --plan "为所有 Controller 生成单元测试"
```

### 3.4 交互反模式（应避免的操作方式）

| ❌ 不推荐 | ✅ 推荐 |
|-----------|--------|
| "帮我重构整个项目"（目标过大） | "把 UserService 中的缓存逻辑抽取到独立的 CacheService" |
| "修复所有 bug"（范围模糊） | "修复 src/auth/login.rs 中的空指针异常" |
| 一次性提交多个不相关的更改 | 每个独立变更为一个单独请求 |
| 不审查 Diff 直接全部接受 | 逐文件审查，确认每处变更 |

---

## 4. VS Code 扩展使用

### 4.1 安装

1. 从 VS Code Marketplace 安装或从 `.vsix` 文件手动安装
2. 确保 `code-agent` CLI 已安装并可用
3. 打开任意项目文件夹

### 4.2 核心功能

| 功能 | 触发方式 | 说明 |
|------|----------|------|
| **Agent 对话** | 侧边栏（Cmd+Shift+P → CraftCoder: Open Chat） | 在侧边栏与 Agent 对话 |
| **内联编辑** | 选中代码 → Cmd+K → 输入自然语言指令 | 选中代码后直接描述修改需求 |
| **智能补全** | 输入时自动触发 | 内联幽灵文本，Tab 键接受 |
| **Bug 修复** | 右键菜单 → "CraftCoder: Explain & Fix" | 选中错误代码，自动分析并修复 |
| **文档生成** | 右键菜单 → "CraftCoder: Generate Docs" | 为当前函数/类生成文档注释 |
| **测试生成** | 右键菜单 → "CraftCoder: Generate Tests" | 为当前函数生成单元测试 |

### 4.3 侧边栏对话

VS Code 侧边栏中的 Agent 对话面板支持：

- 多轮对话（保留上下文）
- 流式输出（实时显示 Agent 推理过程）
- Diff 预览（变更在编辑器中高亮显示）
- 一键接受/拒绝变更
- 工具调用可视化（正在读取文件、执行 Shell 等）

### 4.4 配置

在 VS Code 设置（`settings.json`）中可配置：

```json
{
  "craftcoder.appServer.path": "code-agent",
  "craftcoder.completion.enabled": true,
  "craftcoder.completion.delay": 300,
  "craftcoder.telemetry.enabled": false
}
```

---

## 5. Web API 使用

### 5.1 启动 Web API 服务

```bash
# 启动 Web API 服务（独立二进制）
code-agent-web

# 或通过 cargo 直接运行
cargo run --release -p code-agent-web
```

Web API 服务默认监听 `0.0.0.0:8080`，提供 REST + SSE 接口。

### 5.2 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/chat` | 提交对话消息，返回 SSE 事件流 |
| `GET` | `/api/sessions` | 列出所有会话 |
| `GET` | `/api/sessions/:id` | 获取会话详情 |
| `DELETE` | `/api/sessions/:id` | 删除会话 |
| `GET` | `/health` | 健康检查 |
| `GET` | `/` | 静态文件服务（Web UI）|

### 5.3 使用示例

#### 健康检查

```bash
curl http://localhost:8080/health
# 响应: {"status": "ok"}
```

#### 提交对话并流式获取结果（SSE）

```bash
curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "用 Rust 写一个斐波那契数列生成器"}'

# SSE 事件流响应:
# event: message_delta
# data: {"content": "我来写一个斐波那契数列生成器..."}
#
# event: tool_call
# data: {"name": "write_file", "arguments": {"path": "src/fib.rs"}}
#
# event: turn_complete
# data: {"final_message": "已生成斐波那契数列生成器，代码在 src/fib.rs"}
```

#### JavaScript/TypeScript 客户端示例

```typescript
// 使用 EventSource 监听 SSE 事件流
const response = await fetch('http://localhost:8080/api/chat', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ message: '用 Rust 写一个斐波那契数列生成器' }),
});

const reader = response.body.getReader();
const decoder = new TextDecoder();

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  
  const text = decoder.decode(value);
  // 解析 SSE 事件...
  console.log('收到:', text);
}
```

---

## 6. 故障排除

### 6.1 常见问题

#### Q: TUI 界面显示乱码

确保终端支持 UTF-8 编码和真彩色（24-bit color）。

```bash
# 检查终端类型
echo $TERM
# 推荐: xterm-256color 或 screen-256color

# Windows Terminal 设置
# 设置 → 配置文件 → 外观 → 颜色方案: Campbell
```

#### Q: Agent 卡在某个工具调用不返回

```bash
# 按 Ctrl+C 中断当前操作
# Agent 会保存已完成步骤并返回部分结果

# 或调整超时配置
code-agent config set timeout_secs 60
```

#### Q: 代码生成质量不理想

```bash
# 1. 确保项目已正确索引
# Agent 会自动索引项目（首次启动时自动执行）

# 2. 调整模型参数
# 在配置文件中设置不同的模型
code-agent config set model gpt-4o

# 3. 确保提供充分的上下文描述
# ❌ "修 bug"
# ✅ "在 src/service/order.rs 中，createOrder 方法在并发调用时偶发数据不一致"
```

#### Q: 无法连接到 LLM API

```bash
# 检查 API Key
echo $LLM_API_KEY

# 测试连通性
curl -I "${LLM_API_BASE_URL}/models" \
  -H "Authorization: Bearer $LLM_API_KEY"

# 检查配置文件
code-agent config show | grep api_key
```

#### Q: VS Code 扩展无法连接 App Server

```bash
# 确保 App Server 正在运行
code-agent app-server

# 检查 App Server 日志
RUST_LOG=debug code-agent app-server
```

### 6.2 获取帮助

```bash
# CLI 帮助
code-agent --help
code-agent tui --help
code-agent exec --help
code-agent config --help
code-agent app-server --help

# 环境诊断
code-agent doctor

# 版本信息
code-agent --version
```

---


